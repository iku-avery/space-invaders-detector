# 👾 SPACE INVADERS DETECTOR 👾


### Installation

```shell
   bundle install
```

### Running Tests

Execute the following command to run the tests:

```
    shell
    bundle exec rspec spec
```
